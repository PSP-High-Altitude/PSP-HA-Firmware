#include "state_est.h"

#include "flight_logic.h"

EstState update_state(SensorData* data, FlightPhase phase) {
    switch (phase) {
        case FP_READY:

            break;

        default:
            break;
    }
}